spring:
  application:
    name: customerservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
management:
  endpoint:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: env